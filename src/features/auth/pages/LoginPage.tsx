import type { FC, ReactElement } from "react";

const LoginPage: FC = (): ReactElement => {
  return (
    <div className="p-4">
      <h1>Login Page</h1>
    </div>
  );
};

export default LoginPage;
